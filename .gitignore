# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Output for test save files etc
outtahere/

# Scenes will be in a separate repo to avoid spoilers and stuff
scenes/

# Assets are available upon request but too big to host on github
assets/
